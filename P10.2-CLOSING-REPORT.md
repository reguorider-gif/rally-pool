# P10.2 投注单校验与 Bet Receipts 生成 — 收口报告

**日期**: 2026-06-03  
**阶段**: P10.2  
**状态**: ✅ 完成 / PASS

---

## 1. 结论

| 项目 | 结果 |
|---|---|
| 是否完成 | **是** |
| 是否通过验收 | **PASS** |
| 是否可进入 P10.3 结算 | **是** |

---

## 2. 备份 / checkpoint
- 备份路径：`/Users/audimacmini/Documents/Playground/.omx/ai-judge/pool-app/`
- Git 提交：沙箱权限限制，未提交
- commit hash：N/A

---

## 3. 新增/修改文件

| 文件 | 操作 | 说明 |
|---|---|---|
| `ops/validate_model_outputs.py` | 新增 | 投注单校验主脚本（~350行） |
| `pool_data.py` | 修改 | 新增 `get_bet_receipts`/`get_bet_receipt`/`get_bet_rejections` |
| `app.py` | 修改 | 新增 3 个 bet API 路由，更新 import |
| `ops/check_pool_data_health.py` | 修改 | 新增 P10.2 健康检查（10 项） |
| `ops/generate_daily_pool_report.py` | 修改 | bet_receipt gap 四态识别 |
| `data/pool/bet_receipts/run-5.json` | 生成 | 投注单（accepted=0） |
| `data/pool/bet_receipts/rejections/run-5.json` | 生成 | 拒收记录（13 条） |
| `data/pool/bet_receipts/index.json` | 生成 | 投注单索引 |
| `data/pool/daily_reports/2026-06-03_run-5.json` | 重生成 | gap 更新 |

---

## 4. Bet Receipt 结果

| round | models_total | structured_receipts | accepted_bets | manual_review_bets | rejected_bets | valid_for_settlement |
|---|---:|---:|---:|---:|---:|---|
| run-5 | 13 | 0 | 0 | 0 | 13 | false |

---

## 5. Rejection 摘要

| reason_code | count |
|---|---|
| `no_structured_bet_receipt` | 7 |
| `model_not_eligible_for_consensus` | 6 |

> 7 个 eligible 模型均缺少结构化投注单（raw_text 为自然语言，无 JSON）。  
> 6 个 ineligible 模型被拒收（context_polluted, placeholder_only, quota_blocked, risk_refusal）。

---

## 6. 日报更新

| gap | P10.1 前 | P10.2 后 |
|---|---|---|
| bet receipts | `missing_bet_receipts` | `bet_receipts_present_but_no_valid_bets` |

---

## 7. API 验收（线上）

| API | 状态 | 数据来源 |
|---|---|---|
| `GET /api/pool/bet-receipts` | ✅ 200 | `bet_receipts/index.json` |
| `GET /api/pool/bet-receipts/run-5` | ✅ 200 | `bet_receipts/run-5.json` |
| `GET /api/pool/bet-rejections/run-5` | ✅ 200 | `bet_receipts/rejections/run-5.json` |
| `GET /api/pool/daily-reports/2026-06-03/run-5` | ✅ 200 | 日报 JSON（gap 含 `bet_receipts_present_but_no_valid_bets`） |

**Vercel URL**: https://pool-g5besw1f6-reguorider-9181s-projects.vercel.app  
**别名**: https://pool-app-one.vercel.app

---

## 8. 验收命令输出

### py_compile
```
✅ py_compile ALL PASSED
```

### dry-run
```
validate_model_outputs
  round_id:       run-5
  date:           2026-06-03
  snapshot_label: T-1h
  dry_run:        True
  models_total:   13
  eligible:       7/13
  structured:     0
  accepted_bets:  0
  manual_review:  0
  rejected:       13
  [dry-run] no files written
```

### 正式 validate
```
  [write] bet_receipts/run-5.json
  [write] bet_receipts/rejections/run-5.json
  [write] bet_receipts/index.json
```

### JSON validation
```
bet_summary: {models_total: 13, accepted_bets: 0, valid_for_settlement: False}
rejection_summary: {rejected_models: 13, reason_counts: {no_structured_bet_receipt: 7, model_not_eligible_for_consensus: 6}}
✅ Step 15.4 JSON validation PASSED
```

### Health check
```
✅ data/pool/bet_receipts/run-5.json: exists
✅ data/pool/bet_receipts/rejections/run-5.json: exists
✅ data/pool/bet_receipts/index.json: exists
✅ summary.models_total = 13
✅ accepted_bets=0 (expected with manual_stub)
✅ No odds=null in accepted receipts
✅ manual_stub missing odds not in accepted
✅ rejections have reason_counts
(all P9.0~P10.1 checks also PASS)
```

### 日报断言
```
gap_names: ['odds_snapshots_present_but_missing_market_coverage', 'missing_settlements', 'missing_daily_pipeline', 'bet_receipts_present_but_no_valid_bets']
✅ Step 15.6 daily report assertion PASSED
```

### 离线 API
```
bet_receipts_index keys: ['version', 'updated_at', 'rounds']
bet_receipt keys: ['version', 'round_id', 'date', ...]
bet_rejections keys: ['version', 'round_id', 'date', ...]
✅ Step 15.7 offline API verification PASSED
```

---

## 9. 风险与残留

| 问题 | 影响 | 建议 |
|---|---|---|
| accepted_bets=0（无结构化投注单） | P10.3 结算无意义 | 模型需输出标准 JSON 投注单（非自然语言），或等待模型补跑产生结构化输出 |
| manual_stub 无真实赔率 | odds 匹配永远失败 | 配置 `THE_ODDS_API_KEY` 后重跑 sync_odds_snapshots |
| risk_summary 仍为 `missing_bet_receipts_or_settlements` | 风险计算不可用 | P10.3 结算后可修复 |

---

## 10. 是否建议进入 P10.3

**是**，理由：
1. P10.2 全部 14 项 PASS 标准已满足
2. Bet receipts 数据结构已建立，API 已上线
3. 即使 accepted_bets=0，P10.3 结算脚本的结构（生成 settlements/run-5.json，closed_sans_bets）仍有意义
4. 禁止项均未违反

**注意**：由于 accepted_bets=0，P10.3 结算应处理 "no bets to settle" 场景，适当生成 closed 状态的 settlement 记录。
